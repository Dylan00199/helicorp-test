# Copy this file to `.env` and fill in real values once a backend exists.
# Vite only exposes variables prefixed with VITE_ to client code.

# Base URL for the future backend API (see src/services/api.js).
# Leave empty while there's no backend -- all API calls fall back to mocks.
VITE_API_BASE_URL=
